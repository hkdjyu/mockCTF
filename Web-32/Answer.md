# ✅ Web-32 解題說明 — PNG tEXt 隱寫

1. 下載 `PNG.png`。
2. 用 `https://metadatakit.com/metadata` / `exif.tools` 查看 PNG metadata。
3. 找到 `Artist`裡的`kqfl{htwwjhy_kqfl_ktw_usl}`。
4. 用 CyberChef 解碼（ROT-13, amount=-5）取得旗標。

## Flag
`flag{correct_flag_for_png}`