# Web-39 Setup — Audio Spectrogram 隱寫

## 目標
手動製作一段音訊，讓 spectrogram 中可看到文字/圖形線索。

## 你要準備的檔案
- 放到 `src/files/beacon.wav`

## 製作方式
1. 用 Audacity、Sonic Visualiser 或其他工具產生/編輯音訊。
2. 將高頻能量排成文字（例如 Base64 片段）。
3. 輸出為 WAV。

## 驗證
- 用 spectrogram viewer 看 `beacon.wav` 是否能看到訊息。